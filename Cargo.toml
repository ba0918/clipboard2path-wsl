[package]
name = "clipboard2path-wsl"
version = "0.1.0"
edition = "2024"

[dependencies]
ctrlc = "3"
image = { version = "0.25", default-features = false, features = ["bmp", "png"] }

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
panic = "abort"
